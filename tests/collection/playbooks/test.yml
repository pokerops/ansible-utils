---
- name: Test playbook
  hosts: all
  gather_facts: false
  tasks:
    - name: Create test file
      ansible.builtin.file:
        path: /tmp/test-collection-file
        state: touch
        mode: "0644"
