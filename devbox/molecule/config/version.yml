---
name: version
permissions:
  contents: read
on:
  pull_request:
    branches: ["main", "master"]
    paths-ignore:
      - ".github/**"
      - "**.md"
      - "**.MD"
jobs:
  version:
    uses: pokerops/ansible-utils/.github/workflows/version.yml@master
